<?php
$data = json_decode(file_get_contents('php://input'), true);
if ($data && isset($data['users'])) {
    $usersFile = 'users.json';
    file_put_contents($usersFile, json_encode(['users' => $data['users']]));
    echo 'OK';
} else {
    http_response_code(400);
    echo 'Error';
}
?>
